# For interfacing with PowerHAL
hal_client_domain(hal_camera_default, hal_power)

# For interfacing with ThermalHAL
hal_client_domain(hal_audio_default, hal_thermal);

#============= hal_audio_default ==============
allow hal_audio_default audio_prop:file { getattr map open read };
allow hal_audio_default default_prop:file { getattr map open read };
